- name: Install common packages
  hosts: web
  become: yes

  tasks:
    - name: Install packages
      ansible.builtin.package:
        name:
          - httpd
          - git
          - vim
        state: present

    - name: start and run appache
      ansible.builtin.service:
        name: httpd
        state: started
        enabled: yes
