- name: Install apache
  hosts: webservers
  become: yes

  tasks:
  - name: Install apache
    ansible.builtin.package:
    #yum
      name: httpd
      state: present
  - name: start and enable service
    ansible.builtin.service:
    #service
      name: httpd
      state: started
      enabled: yes