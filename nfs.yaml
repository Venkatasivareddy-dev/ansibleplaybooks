- name: Install nfs packages
  hosts: web
  become: yes

  tasks:
    - name: Install nfs packages
      ansible.builtin.package:
        name: nfs-utils
        state: present
      
    - name: start nfs and enabled 
      ansible.builtin.service:
        name: nfs-utils
        state: started
        become: yes