- name: copyfiles
  hosts: web
  become: yes

  tasks:
  - name: copy files from source file on ansible server to destination on target servers
    copy:
      src: /home/ec2-user/ansibleplaybooks/index.html
      dest: /tmp