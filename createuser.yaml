- name: create user
  hosts: web
  become: yes

  tasks:
  - name: create user
    ansible.builtin.user:
      name: venkat
      password: "{{ 'Welcome@2026' | password_hash( 'sha512' ) }}"
      state: present